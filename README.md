# webserver_onion
## Install docker-compose by: 
  sudo apt-get -y install docker-compose
## The HOST and DATABASE parameters:
  All the parameters are defined in docker-compose.yml
## Go: 
  sudo docker-compose up
